Designed a SR latch with enable and reset as controlling signal\
Reset is implemented to store the assign previous values of q and qbar\
Enable acts as clock\
s and r are in the inputs\
q and qbar are the outputs
